{
  "name": "react-clock-app",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "deploy": "^1.0.3",
    "react": "^16.4.0",
    "react-dom": "^16.4.0",
    "react-scripts": "1.1.4"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test --env=jsdom",
    "eject": "react-scripts eject",
    "predeploy": "npm run build",
    "deploy": "gh-pages -d build"
  },
  "homepage": "http://deadfrominside.github.io/react-clock-app",
  "devDependencies": {
    "gh-pages": "^1.2.0"
  }
}
